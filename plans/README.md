# Implementation Plans

Generated by the improve skill on 2026-07-11 at commit `bff3b6b`. Execute in the
order below unless dependencies say otherwise. Each executor: read the plan
fully before starting, honor its STOP conditions, and update your row when done.

Baseline at planning time: `bun run check-types`, `bun run test` (93 tests in
`@kkb/web` plus `@kkb/audio`/`@kkb/ui`/`@kkb/ableton` suites), and
`bun run format-and-lint` all pass.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Add verification CI workflow (check-types, test, format-and-lint) | P1 | S | — | TODO |
| 002 | Move `shadcn` CLI out of `@kkb/ui` runtime dependencies | P1 | S | — | TODO |
| 003 | Make `bun test` fail loudly in workspaces that have tests | P2 | S | — | TODO |
| 004 | Characterization tests for `BinauralBeatsClient` | P2 | M | — | TODO |
| 005 | Eliminate per-frame allocations in oscilloscope render loop | P2 | M | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- No hard dependencies. Recommended order: 001 first (it is the regression net
  every other plan's done criteria lean on), then 002/003 (small, independent),
  then 004, then 005 (the only MED-risk change, best landed with CI in place).
- 003 before 004 is mildly preferable: once `apps/web` drops
  `--pass-with-no-tests`, the new binaural suite cannot silently vanish.

## Findings surfaced but not planned this run

Vetted findings the maintainer chose not to plan yet (re-run `/improve` or ask
for a `plan <description>` to pick any up):

- **json-render engine untested** — `packages/ui/src/json-render/{registry,catalog}.ts` have no tests; S effort.
- **`focused-specimens.tsx` god file** — 2,225 lines, ~15× repo median; split into per-category modules; M effort.
- **Catalog search re-normalizes per keystroke** — `apps/web/components/ui-catalog/catalog-search-index.ts:54-111` re-runs regex normalization over all items on every input; precompute; S effort.
- **Duplicated lint-as-test** — `audio-theme.test.ts` source-regex checks copy-pasted in `apps/web` and `packages/ui`; convert to a lint rule or one shared helper; S effort.
- **AGENTS.md/CLAUDE.md omit `packages/ableton`** — incomplete workspace map in the primary agent guidance files; S effort.
- **`/ui` route bundles all catalog sections statically** — `category-surface.tsx:8-16`, `preview-wall.tsx`; convert to lazy imports; M effort.
- **Five font families preload globally** — `apps/web/app/layout.tsx:7-30`; scope per route, convert `GeistVF.woff` to woff2; S effort.
- **`claude.yml` grants `contents: write` on `@claude`-triggered issue events** — dormant while the repo is private; add an `author_association` gate before any move to public; S effort.

## Findings considered and rejected

(So nobody re-audits these.)

- **WebGPU renderer not calling `device.destroy()`** (`pipeline.ts:277-283`): by design — decided with rationale in `docs/plans/2026-04-25-oscilloscope-renderer-post-merge-hardening.md:132-150` and locked by the test at `renderer/__tests__/pipeline.test.ts:175`. Revisit trigger per that doc: browser profiling showing retained resources, or an injected-device path.
- **Derived-stereo "double conditioning"** (`analyser-source.ts:126`): intentional — the second pass renormalizes the transformed signal back to `targetPeak`; both channels converge on the same peak.
- **ui-catalog surface consolidation**: recent history (`refactor: split/simplify ui catalog surfaces`) shows deliberate convergence; LOW-confidence smell only.
- **`use-player-controller.ts` 11-line module**: not worth doing.
- **Oscilloscope controls/shell tests**: likely presentational; revisit only if logic accretes there.
- **Minor dependency drift** (biome/lucide/@types patch bumps, root TS pin form, `react-day-picker` v10, turbo schema URL): routine; batch with the next dependency chore.
- **`@base-ui/react` used only by combobox**: likely a deliberate exception (radix has no combobox primitive); worth a one-line note in `packages/ui` docs, not a plan.
- **Direction options** (oscilloscope session extraction, Ableton extension growth, Convex persistence spike, docs-app content pipeline): surfaced with evidence during the 2026-07-11 audit; maintainer deferred all four — see the audit summary in the conversation, `docs/reports/2026-04-23-codebase-architecture-deepening-candidates.md`, and `docs/reports/2026-04-29-audio-package-architecture-vision.md`.
