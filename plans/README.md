# Implementation Plans

Generated by the improve skill on 2026-07-11 and reconciled on 2026-07-15
against commit `704eeb9`. Execute in the order below. Each executor: read the
plan fully before starting, honor its STOP conditions, and update your row when
done.

Baseline at reconciliation time: `bun run check-types` passes with 5 tasks;
`bun run test` passes with 96 `@kkb/web`, 81 `@kkb/audio`, and 16 `@kkb/ui`
tests while `@kkb/ableton` intentionally has no tests; and
`bun run format-and-lint` exits 0 with 31 pre-existing warnings and 3 infos.

## Queue location

Root `plans/` is the improve-generated execution queue and status ledger for
this five-PR sequence. It is a deliberate exception to the repository's normal
documentation map: `docs/plans/` remains canonical for durable architecture and
project plans, while this directory holds short-lived agent handoffs whose
paths and status rows are updated as they execute. Do not move these files
during the sequence; their dependency links and task goals refer to them here.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Add verification CI workflow (check-types, test, format-and-lint) | P1 | S | — | DONE |
| 002 | Move `shadcn` CLI to `@kkb/ui` development dependencies | P1 | S | 001 | DONE |
| 003 | Make `bun test` fail loudly in workspaces that have tests | P2 | S | 002 | DONE |
| 004 | Characterization tests for `BinauralBeatsClient` | P2 | M | 003 | TODO |
| 005 | Reuse typed-array buffers in the oscilloscope render loop | P2 | M | 004 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- Execute and merge the plans serially: 001 -> 002 -> 003 -> 004 -> 005.
- 001 supplies the CI regression net used by every later PR.
- 002 and 003 both edit `packages/ui/package.json`; landing 002 first avoids
  manifest and plan-index conflicts.
- 003 must land before 004 so the new binaural suite cannot silently disappear.
- 005 has no code dependency on 004, but it stays last because it is the only
  MED-risk runtime change.

## Branch, worktree, and PR policy

- The operator explicitly authorized dedicated worktrees for this queue on
  2026-07-15. This is the required exception to any general agent default that
  avoids creating worktrees without operator approval.
- Give every plan its own `codex/plan-NNN-<slug>` branch and dedicated worktree.
- Create each branch from updated `main` only after the preceding plan PR is
  merged; do not stack the plan PRs unless the operator explicitly changes this
  policy.
- Run the plan's targeted checks and the full verification loop before pushing.
- Open exactly one PR per plan. Do not merge without explicit operator approval.
- The supervising task reviews diffs and verification independently but does not
  edit, commit, push, or merge implementation branches.

## Verification toolkit

- Automated checks named in each plan remain mandatory and are the primary
  acceptance gates.
- Use Codex's Browser plugin for local-route interaction, visible-state checks,
  screenshots, and browser verification where relevant, especially Plans 004
  and 005.
- Use Codex's Computer Use plugin when verification requires a native browser or
  OS surface the Browser plugin cannot expose, such as DevTools profiling or a
  permission prompt. Follow its action-time confirmation requirements.
- The supervising task should independently repeat material Browser or Computer
  Use observations instead of accepting the implementation task's summary.

## Findings surfaced but not planned this run

Vetted findings the maintainer chose not to plan yet (re-run `/improve` or ask
for a `plan <description>` to pick any up):

- **json-render engine untested** — `packages/ui/src/json-render/{registry,catalog}.ts` have no tests; S effort.
- **`focused-specimens.tsx` god file** — 2,225 lines, ~15× repo median; split into per-category modules; M effort.
- **Catalog search re-normalizes per keystroke** — `apps/web/components/ui-catalog/catalog-search-index.ts:54-111` re-runs regex normalization over all items on every input; precompute; S effort.
- **Duplicated lint-as-test** — `audio-theme.test.ts` source-regex checks copy-pasted in `apps/web` and `packages/ui`; convert to a lint rule or one shared helper; S effort.
- **AGENTS.md/CLAUDE.md omit `packages/ableton`** — incomplete workspace map in the primary agent guidance files; S effort.
- **`/ui` route bundles all catalog sections statically** — `category-surface.tsx:8-16`, `preview-wall.tsx`; convert to lazy imports; M effort.
- **Five font families preload globally** — `apps/web/app/layout.tsx:7-30`; scope per route, convert `GeistVF.woff` to woff2; S effort.
- **`claude.yml` grants `contents: write` on `@claude`-triggered issue events** — dormant while the repo is private; add an `author_association` gate before any move to public; S effort.

## Findings considered and rejected

(So nobody re-audits these.)

- **WebGPU renderer not calling `device.destroy()`** (`pipeline.ts:277-283`): by design — decided with rationale in `docs/plans/2026-04-25-oscilloscope-renderer-post-merge-hardening.md:132-150` and locked by the test at `renderer/__tests__/pipeline.test.ts:175`. Revisit trigger per that doc: browser profiling showing retained resources, or an injected-device path.
- **Derived-stereo "double conditioning"** (`analyser-source.ts:126`): intentional — the second pass renormalizes the transformed signal back to `targetPeak`; both channels converge on the same peak.
- **ui-catalog surface consolidation**: recent history (`refactor: split/simplify ui catalog surfaces`) shows deliberate convergence; LOW-confidence smell only.
- **`use-player-controller.ts` 11-line module**: not worth doing.
- **Oscilloscope controls/shell tests**: likely presentational; revisit only if logic accretes there.
- **Minor dependency drift** (biome/lucide/@types patch bumps, root TS pin form, `react-day-picker` v10, turbo schema URL): routine; batch with the next dependency chore.
- **`@base-ui/react` used only by combobox**: likely a deliberate exception (radix has no combobox primitive); worth a one-line note in `packages/ui` docs, not a plan.
- **Direction options** (oscilloscope session extraction, Ableton extension growth, Convex persistence spike, docs-app content pipeline): surfaced with evidence during the 2026-07-11 audit; maintainer deferred all four — see the audit summary in the conversation, `docs/reports/2026-04-23-codebase-architecture-deepening-candidates.md`, and `docs/reports/2026-04-29-audio-package-architecture-vision.md`.
